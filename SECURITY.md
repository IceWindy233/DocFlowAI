# 安全与数据披露说明

DocFlow AI 面向本地单机部署，不提供生产环境安全承诺。请不要在 Issue、日志或截图中提交 API Key、
真实公文、个人信息、合同、财务数据、数据库文件或解析产物。

## 密钥处理

- 只在本机 `.env` 中配置密钥；仓库只提供空值的 `.env.example`；
- 如果密钥曾进入提交历史、终端共享记录或公开截图，应立即在供应商控制台撤销并重新生成；
- 配置中心只保存环境变量名称，不保存或回显密钥值。

## 数据边界

- `examples/demo-corpus` 是唯一可随仓库分发的演示语料，内容全部虚构；
- 自有语料、数据库、向量索引、页面图片、OCR 结果和私有评测集不得提交；
- 在导入第三方文档前，请确认拥有处理权限，并遵守所在组织的数据安全要求。

## 报告问题

公开仓库建立后，请通过不含敏感数据的 GitHub Issue 描述普通缺陷。涉及潜在密钥泄露、路径穿越、
任意文件读取或敏感信息回显的问题，不应粘贴真实样本；请使用最小化的虚构复现材料。
