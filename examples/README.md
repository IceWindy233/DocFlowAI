# 脱敏演示语料

本目录只包含为公开仓库编写的虚构材料，不对应任何真实单位、项目或个人。内容用于验证文件盘点、
文本解析、知识问答、公文审核和公文撰写的基本链路，不能作为真实业务依据。

建议在 Web 管理中心把 `examples/demo-corpus` 作为数据源，完成解析、校验和发布后，再运行
`evaluation/fixed-agent-samples-public-v1.json` 中的公开评测样例。

真实公文、数据库、页面图片、OCR 结果和私有 Publication 评测集均由 `.gitignore` 排除。
